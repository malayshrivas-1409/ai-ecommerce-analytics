version: "3.9"

services:

  generator:

    build: .

    container_name: e-commerce-gen

    environment:

      BUCKET_NAME: malay-comm-datalake-2026

      MAX_EVENTS_PER_FILE: 10

    volumes:

      - ./logs:/app/logs

    restart: unless-stopped